pipeline {
    agent { label 'docker' }

    stages {
        stage('Test') {
            steps {
                sh './gradlew -version'
                sh 'java -version'
            }
        }
    }
}
